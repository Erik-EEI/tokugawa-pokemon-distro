import './DashboardSectionTitle.css';

const DashboardSectionTitle = ({ sectionTitle }) => {
  return (
    <h2 className={'dashboard-section-title'}>{sectionTitle}</h2>
  );
};

export default DashboardSectionTitle;
